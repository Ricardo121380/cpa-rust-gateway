# P13-10 public Responses WebSocket phase review — 2026-08-16

Status: `DONE_WITH_BOUNDARY`

## Review conclusion

P13-10A implementation and the aggregate 43-step local Full preflight are complete. The slice
review and final phase review found and closed the material authentication, framing,
resource-bound, cancellation, capability, continuity, dependency-boundary and secret-projection
issues. No blocking code, contract, dependency, security or local-test finding remains inside the
accepted P13-10A boundary.

## Goal-backward review

| Required property | Review result |
|---|---|
| Upgrade admission | Exact existing Client Key authentication happens before `101`; duplicate/conflicting credentials fail closed, any Origin is rejected, optional turn state is single and bounded, and all handshake outcomes are `no-store`. |
| Strict client protocol | Only UTF-8 text `response.create` is accepted; duplicate JSON names, `generate`, unknown event types, binary frames and `stream:false` fail closed. The WebSocket envelope `type` is removed and is never forwarded upstream. |
| One execution owner | WebSocket turns enter the same model view, RouteSnapshot, scheduler, Credential pool/lease, Health/Quota, Attempt orchestration, egress, usage and Canonical path as HTTP/SSE. No second selector or Provider executor exists. |
| Capability isolation | `ResponsesClientTransport::WebSocket` requires exact candidate capability `responses_websocket` before lease/Provider execution; downstream WebSocket does not invent upstream-native WebSocket support. |
| Bounded session | Frame/message/fragment, event/capture/output, active/pending, write, event-idle, Pong, idle, turn and connection lifetime limits are finite; a third pending request closes `1008` and cancels the active turn. |
| Cancellation and lease | Peer close, disconnect, malformed framing, timeout or backpressure aborts the active task; dropping the bounded Canonical source releases its Credential lease and no queued turn starts after closure. |
| Canonical output | Existing Responses encoder events are emitted as one JSON text message each, without SSE framing. First-semantic delivery is recorded only after the corresponding write succeeds; StreamError/truncation cannot become completed. |
| Stored success | `store:true` retains P13-09 exact owner, AEAD and durability-before-terminal semantics; partial, malformed, timed-out or failed turns are not inserted as completed connection roots. |
| Exact continuity | Same-session replay is bounded and carries Config/Provider/Upstream/Channel/Route/Candidate/Credential revision; cache misses use the existing durable Client-Key owner. Neither path forwards a downstream locator or falls back across Credential/Provider. |
| Surface isolation | P13-10 changes the public Responses client surface only. Management OpenAPI/Prism, production/staging/server state and the four untracked helpers remain unchanged; Claude Code was notified through the cross-boundary log. |

## Corrective review record

- The first local Fast pass exposed that the architecture allow-list had not yet named runtime
  `actix-ws` and test-only `tokio-tungstenite`; the allow-list and crate-boundary document were
  corrected, and the boundary plus every remaining tail check passed.
- Control-frame writes were brought under the same bounded write timeout as data frames.
- Actix frame overflow and invalid UTF-8 were mapped to the closed `1009` and `1007` classes.
- Connection-local response-ID replay was made idempotent only for an identical completed turn;
  a conflicting model/request/response/lineage fails closed instead of replacing state.
- Real loopback tests prove unauthorized `401`, Origin `403`, authenticated `101`, turn-state echo,
  lifecycle JSON, same-session exact continuation, pending overflow `1008`, and active-source drop.

## Local verification review

- Aggregate Full preflight: `43/43` steps passed on `Darwin 25.2.0 arm64`, from
  `2026-08-16T04:26:59Z` through `2026-08-16T04:28:58Z`.
- P13-10A affected-package evidence includes protocol-openai-responses `30`, gateway-catalog `15`,
  gateway-router `138`, gateway `106`, and gateway-http-actix `65` unit tests, plus all applicable
  integration, component-smoke and real loopback WebSocket tests.
- Strict workspace Clippy, format, source policy, crate boundaries, docs/contracts, tracked Secret
  scan, dependency policy and RustSec all passed.
- Management contract/Prism remained synchronized at 83 operations, with no P13-10 management
  OpenAPI or frontend-source change.

## Non-blocking evidence boundaries

- Deterministic local fixtures do not claim that a live Provider account is currently available or
  that a Provider supports an upstream-native WebSocket transport.
- No real Provider, public CPAR base URL, staging/production deployment, credential refresh/reauth,
  server, DNS or Caddy mutation was used by this phase.
- Browser Origin policy, Realtime, `response.append`, Chat/Messages WebSocket and Provider-native
  WebSocket remain separate contracts; the phase Gate is not rollout authorization.

These are explicit evidence boundaries, not correctness exceptions and not permission to add an
implicit capability, credential or Provider fallback.

## Formal Delivery Gate review

- Immutable tag: `phase-p13-websocket-complete`.
- Exact closeout commit: `dc48ec40e4fb38961925f203bf3cd0f7434a34a0`.
- Formal run:
  [31926927914](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/31926927914).
- Job results: Authorize `success` in 4s; Fast `success` in 5m51s; Full supply-chain `success`
  in 1m33s; Required `success` in 3s.
- The run emitted only non-blocking GitHub Node.js 20 deprecation annotations for pinned
  `actions/checkout` and `actions/cache`; no code, contract, supply-chain or test finding failed.

The tag resolves to the exact closeout commit above and was not moved or recreated. The successful
formal run accepts the reviewed P13-10 result without broadening an implementation or operational
boundary.

## Closeout boundary

No Provider request, credential operation, server mutation, production/staging traffic or
management OpenAPI/Prism change was performed. P13-10A and the aggregate close as
`DONE_WITH_BOUNDARY`; P13-08 remains separately pending its own phase boundary, P13-11 was not
started, and the P13 umbrella remains `IN_PROGRESS` because later independent backlog exists.
