# P13-11 generic egress phase review — 2026-08-17

Status: `READY_FOR_FORMAL_DELIVERY_GATE`

## Review conclusion

The P13-11A/B/C implementation, slice reviews and aggregate local Full preflight are internally
consistent. No remaining P1/P2 correctness, ownership, lease-lifetime, Provider-isolation or
secret-projection finding was identified inside the accepted generic egress boundary. The phase is
ready for one formal Delivery Gate on an exact pushed commit, but it is not marked done before that
Gate.

## Goal-backward review

| Required property | Review result |
|---|---|
| Generic endpoint neutrality | CPA JSON, Sub2API JSON, direct OAuth, plain API-key and custom/base-url relays are represented by the same typed endpoint binding; the label `Krill` does not select a special transport path. |
| Single Credential owner | `RouteCredentialScheduler` remains the only Credential lease owner. P13-11C validates that exact live lease and acquires only egress capacity; no second Credential selection or cursor advance is introduced. |
| Config and ownership isolation | The runtime is compiled from the active Config Version and exact Upstream/Endpoint/Policy/Credential lineage. Foreign Upstream, Endpoint, policy, revision, schedule, Health, Quota, expiry or capacity state fails closed. |
| Transport preservation | Direct/local-DNS SOCKS5 selection overlays the existing JSON/SSE mode-specific profile without weakening connect, TTFB, idle, total or pool limits. The egress lease remains held through source completion, cancellation, error or drop. |
| Failure feedback | Pre-response transport failures retain Endpoint, Endpoint-Credential and Egress-Node scope and are projected as the closed compatible-egress category; the ordinary orchestrator does not add a duplicate Endpoint cooldown. |
| Sticky behavior | `CredentialAndEgress` stickiness is exact to one Credential/Endpoint/Upstream and process-local. An unavailable retained node fails closed rather than silently rotating or crossing Provider boundaries. |
| Provider boundaries | Generic OpenAI Chat/Responses and Anthropic Messages are the only serving handoff families. Native Grok/Kiro, browser clearance, hidden auxiliary HTTP, Autoreg and Provider-specific recovery remain outside this phase. |
| Secrecy | Debug, error, observation, event and test projections exclude endpoint/proxy URLs, API keys, OAuth/SSO material, cookies, bodies, Provider responses and DNS answers. |
| Side effects | Local tests use deterministic/loopback fixtures only. No Store hot-path read, Provider call, DNS/proxy probe, server mutation, public API or management OpenAPI/Prism change is claimed. |

## Verification reviewed

- Aggregate Full preflight: `43/43` steps passed on `Darwin 25.2.0 arm64`; receipt:
  `p13-11-phase-preflight-20260817.md`.
- Slice suites and strict Clippy: `gateway-upstream` `37`, `gateway-router` `151`,
  `gateway-control` `72`, and `gateway` `106`; the aggregate Full also passed format, source,
  crate-boundary, docs, contracts, secret, dependency and RustSec checks.
- Four pre-existing untracked helper files remained outside the reviewed candidate.

## Decision and next boundary

P13-11 aggregate is `READY_FOR_FORMAL_DELIVERY_GATE`, not `DONE_WITH_BOUNDARY`. The next authorized
closeout action is one annotated phase tag on the exact pushed commit, followed by the single formal
Fast/Full/Required Delivery Gate. Until that external action is explicitly authorized, do not create
the tag, push, contact Providers, alter staging/production, or begin P13-11D.

After a successful formal Gate, P13-11D may be planned as a separate protected
Config-Version proxy-pool persistence/management slice. Provider-specific probes, browser
clearance, recovery and Autoreg remain independent boundaries even then.
