# P13-07 routing phase review — 2026-08-15

Status: `DONE_WITH_BOUNDARY`

## Review conclusion

The P13-07A/B/C/D implementation, local Full preflight and immutable formal Delivery Gate are
complete. Independent reviews found and closed the material issues in Provider-scoped quota
recovery, price-policy lineage validation, Prism PUT generation, required Route Explain evidence,
cursor/runtime status consistency and documentation attribution. No blocking code, contract,
security or test finding remains within the accepted P13-07 boundary.

## Goal-backward review

| Required property | Review result |
|---|---|
| Provider isolation | Foreign Providers are excluded before lease and quota-recovery paths retain the exact Provider scope. |
| Deterministic selection | Health, Quota, capability, expiry, concurrency, price evidence, load, priority, weight and opaque identity have bounded deterministic ordering. |
| Exact lease ownership | Advisory ordering never substitutes for the existing scheduler lease; Health, Quota, expiry, binding and capacity are revalidated on the same runtime objects. |
| Retry/FSE preservation | `max_attempts`, first-semantic-event, cancellation, timeout, attempt exclusion and post-semantic no-retry boundaries remain owned by the AttemptOrchestrator. |
| Price semantics | Six rates are never collapsed into a fabricated request cost; crossed vectors remain incomparable and missing tuples remain unpriced, not zero. |
| Config lifecycle | The routing price policy is draft-only, revisioned, audited and exact to one immutable catalog and Config Version. |
| Serving/Explain parity | Runtime composition injects one immutable price map into the serving scheduler and Route Explain facade. |
| Management contract | OpenAPI and Prism expose required nullable policy lineage and one closed evidence value per candidate; PUT generation and drift checks are retained. |
| Secret boundary | No credential value, URL, request/response body, token, digest or raw Provider error enters the routing evidence or receipt. |

## Corrective review record

- P13-07C's first review found that the legacy quota-recovery fallback did not carry the selected
  Provider scope. The provider-scoped recovery path now checks hard eligibility, caller admission,
  exact Provider ownership and binding exclusion before lease; foreign recovery-due regression has
  zero lease and zero driver starts.
- P13-07D's review rejected a scalar request-cost estimate because request-start Usage is unknown.
  The implementation instead carries six exact rates and closed dominance evidence.
- Route Explain now validates bounded catalog lineage, requires `price_policy` with `null` meaning
  disabled, and requires closed `price_evidence` for every candidate.
- Prism's generator and compatibility checks now retain PUT, while the cross-boundary log assigns
  those changes to P13-07D and leaves Claude Code an explicit display-only handoff.
- ADR, Contract and traceability statuses were held at `LOCAL_PASS_PENDING_PHASE_GATE` until the
  formal tagged run succeeded; post-Gate reconciliation can now close them as
  `DONE_WITH_BOUNDARY`.

## Non-blocking evidence boundaries

- A dedicated malformed/missing catalog-row composition fixture remains optional because normal
  persisted policy rows are protected by SQLite foreign keys and compiler/Store tests cover the
  fail-closed invariant.
- Additional active/archived/unknown-catalog HTTP permutations would deepen boundary evidence, but
  Store/service/parser tests and the current HTTP future/stale/CSRF matrix cover the operative
  safety rules.
- Formal Prism page implementation is intentionally deferred to Claude Code; generated contract
  synchronization and cross-boundary instructions are complete.

These are evidence-expansion opportunities, not correctness exceptions and not authorization to
weaken the fail-closed behavior.

## Formal Delivery Gate review

- Local Full preflight: `43/43` steps passed on `Darwin 25.2.0 arm64`, from
  `2026-08-15T08:52:23Z` through `2026-08-15T08:54:28Z`.
- Immutable tag: `phase-p13-routing-complete`.
- Exact closeout commit: `0c338ee8eef76e470c55515a24728324684365c5`.
- Formal run:
  [31875826495](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/31875826495).
- Job results: Authorize `success` in 3s; Fast `success` in 5m57s; Full supply-chain `success`
  in 1m16s; Required `success` in 2s.
- The run emitted only non-blocking GitHub Node.js 20 deprecation annotations for pinned
  `actions/checkout` and `actions/cache`; no blocking workflow, supply-chain or test finding was
  reported.

The tag resolves to the exact closeout commit above and was not moved or recreated. The successful
formal run accepts the reviewed P13-07 result without broadening any implementation or operational
boundary.

## Closeout boundary

No Provider request, credential operation, server mutation, production/staging traffic, proxy
change or public inference API change was performed. Gate success closes only P13-07 with explicit
UI/egress/reauth/backlog boundaries. The P13 umbrella remains `IN_PROGRESS`, and P13-08 is not
started by this review. The final P13-07 disposition is `DONE_WITH_BOUNDARY`.
