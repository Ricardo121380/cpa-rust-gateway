# P12-05 CR-013 Messages lifecycle repair review

| Field | Result |
|---|---|
| Scope | P12 runtime only: non-streaming OpenAI Responses decoding for the isolated Staging path. No generic Provider, pure Anthropic codec, HTTP listener, route, retry, Credential, egress, or proxy contract changed. |
| Diagnostic basis | The single CR-012 transaction recorded a successful `decoder`-stage attempt after its one Messages request, then restored the Staging preimage. This establishes a completed upstream decode and constrains the repair to the subsequent Messages response lifecycle. |
| Usage order | Reported input usage is parsed before visible output. Only a real `input_tokens` value produces an input-only non-final delta before `MessageStart`; missing usage remains unrepresented and therefore fails closed at the Messages boundary rather than being estimated. |
| Completion semantics | The decoder closes the message before final usage, emits final usage before `ResponseEnd`, maps text completion to `end_turn`, and maps completed Function Calls to `tool_use`. |
| Cross-protocol usage | The P12 Messages marker is the existing trusted `anthropic.messages.max_tokens` Canonical extension. Responses callers retain detailed final usage; Messages callers retain the representable totals and cache-input counts while omitting the two OpenAI-only sub-counters with no Anthropic field. |
| Credential and transport boundary | The projection is local data shaping. It reads no secret, creates no HTTP request, alters no target, egress admission, header, timeout, proxy, retry, route selection, listener, or database state. |
| Focused boundary regressions | `cargo test -p gateway p12_decoded -- --nocapture` — PASS (3 tests): text and Tool completions encode through real Actix Messages handling, including an upstream reasoning sub-counter; OpenAI Responses retains that detailed counter. |
| Package and lint | `cargo test -p gateway` — PASS (22 unit + 1 component test); `cargo clippy -p gateway --all-targets --all-features -- -D warnings` — PASS. |
| Full local gate | `env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy ./scripts/check.sh full` — PASS. It covered plan/workflow guards, format, workspace Clippy/tests, P12 envelope, source/crate policy, document links, tracked Secret scan, dependency policy, and a fresh direct RustSec audit. |

## Review conclusion

This separate review pass found no source-level blocker. The repair is confined to the P12
non-streaming output bridge and adds the exact lifecycle facts required by the Anthropic Messages
encoder without inventing usage or changing the upstream request. The projected Messages usage
preserves all fields that protocol can express; OpenAI Responses continues to receive the original
detailed usage representation.

The prior proxy-environment RustSec fetch error was isolated to the local development environment:
the public advisory source and direct Git access succeeded, and the required fresh direct audit
passed in the final Full gate. It is not a source, dependency, Credential, or Staging failure.

The only next permitted action is to commit this exact reviewed source, create and independently
verify its new private GitHub OIDC/Sigstore artifact, then run exactly one fresh isolated Staging
Messages validation with mandatory rollback. Tool, Explain, direct probes, P12-06, and public
exposure remain prohibited.
