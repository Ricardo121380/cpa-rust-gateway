# P12-05 CR-004 local compatibility review

| Field | Result |
|---|---|
| Scope | P12-only fixed, non-secret Krill/Codex compatibility `User-Agent`; generic OpenAI-compatible Provider unchanged |
| Credential handling | No refresh or re-login; no endpoint, credential, OAuth value, model, request body, or response body recorded |
| Focused regression | `cargo test -p gateway p12_transport_headers -- --nocapture` — PASS (one exact-header test) |
| Package tests | `cargo test -p gateway --all-targets` — PASS (14 unit + 1 component test) |
| Lint | `cargo clippy -p gateway --all-targets -- -D warnings` — PASS |
| Full local gate | `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=http.version GIT_CONFIG_VALUE_0=HTTP/1.1 CHECK_REPORT_PATH=/tmp/p12-05-cr-004-final-full-http11-20260725.md ./scripts/check.sh full` — PASS (plan/workflow guards, format, workspace Clippy/tests, source and crate policy, document links, tracked Secret scan, dependency policy, and RustSec audit) |
| Network note | A prior identical Full run reached `cargo deny` then hit a transient GitHub TLS handshake error through the existing local Git proxy. The final successful run used a process-only HTTP/1.1 override; no global Git, proxy, Clash, repository, or credential configuration was changed. |
| Required follow-through | Documentation/Secret closeout gates, commit, exact-SHA artifact, independent artifact verification, and only then P12-05 Staging graph work |

## Review conclusion

The request builder continues to own URL construction, API-key formatting, request serialization,
and direct-egress admission.  The P12 adapter borrows that outbound request, rejects an admitted
target that is not byte-for-byte the request's parsed URL, copies its three required headers and
body into the existing admitted direct transport request, then appends only the fixed compatibility
header.  Its regression proves the method/body preservation and the mismatch rejection.  There is
no generic Provider behavior change and no path to add an unreviewed header, retry, redirect,
credential source, or egress target.

The initially reported Clippy ownership finding was corrected by borrowing the outbound request.
The target-binding review correction has its own focused and package evidence above; the final
Full Gate passed. Documentation/Secret closeout checks run after this evidence entry is finalized.
