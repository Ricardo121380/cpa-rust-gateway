# P12-08G1 final review

| Field | Value-free result |
|---|---|
| Exact revision | `ec2fdf6f1ae88d857f4865daf9f9349b22696961` |
| Signed workflow | GitHub Actions run `30767237127`; x86_64 and aarch64 succeeded |
| ARM64 verification | Structural verifier, independent Sigstore `verify-blob`, ELF architecture and transfer digest passed |
| Resume boundary | Began from ten prior PASS tuples; no prior PASS was resent |
| Final sends | Tuple 11 Messages JSON Tool once; tuple 12 Messages SSE Tool once |
| Final semantics | Both `PASS`, projection `tool`, terminal `tool_use`, Usage valid |
| Durable Attempts | Two new Attempts; both `succeeded` |
| Matrix result | 12/12 PASS |
| Rollback | G1 key rejected; predecessor key accepted; predecessor graph restored |
| Service boundary | Active, disabled at boot, only loopback data and management listeners |
| Cleanup | One-time G1 key and temporary graph/harness/classifier scripts removed; absence verified |
| Excluded state | CPA, Caddy, Cloudflare, DNS, public traffic and CC Switch unchanged |

The first CR-036 client attempt before the corrected restart did not create an upstream Attempt:
the executor was intentionally stale after Registry-only graph reactivation. The corrected
transaction restarted after activation and again after rollback. No endpoint, credential, model,
request/response body, identifier, timestamp, token value or fingerprint is retained.

This closes G1 only for the currently available Codex/Krill channel. Claude, Kiro and Grok Official
live availability remain unclaimed or Deferred, and this receipt does not authorize P12-09.
