# CPAR 完整对齐实施进度

2026-09-13。本文件是阶段记录，不是最终交付报告；完整计划尚未完成，尚未上线。

## 已提交

- `7e8dfc7`：按账号刷新真实上游目录；兼容目录解析与有界分页；目录总数；禁止发现模型自动开放与继承权限；旧空实现明确返回不支持；P12 空格变体展示修正；原生账号实际协议；密钥全选当前开放模型。
- `c19c91c`：提供商紧凑列表、独立接口/账号工作区、统一手机卡片与可见操作；高级调度折叠。

新增权威接口为 `POST /admin/catalog/refresh`，只提交接口/账号 ID，不提交 Client Key 或 Provider secret。真实生产目录核对尚未完成，不能把接口实现当作真实渠道验收结果。

## 本次验证

- 前端288项单测、类型检查、构建及131操作的四文件双构建门禁通过。
- 路由179项、HTTP库71项、目录/管理资源21项回归通过；gateway132项与组件冒烟1项通过。
- EgoLite 空间16：隔离演示数据下走通提供商创建与详情展开，检查1440×900浅色、1280×720深色、390×844手机。手机详情操作不超出视口。这些是局部视觉证据，不是全部栏目或真实授权验收。
- Oracle只读核对：服务仍active；活动配置仍为`production-models-20260913`、`rev-12`，四个配置模型及四个提供商。生产名称确实仍含P12；未执行生产迁移、别名删除或发布。

## 必须继续完成

1. 普通/原生统一账号投影、全量搜索排序分页、渠道授权导入与额度流程。
2. Grok Build/Krill真实完整目录核对，按账号隔离、跨页选择与批量部分失败。
3. 固化线上实际服务与授权清单，生产副本演练正式命名和六个旧别名删除，验证回滚。
4. 请求终态、真实延迟、持久列表/聚合/导出与仪表盘运营分析。终态必须覆盖客户端编码/发送失败，不能只记录上游流结束。
5. 剩余栏目统一操作、未保存保护、价格工作流和全站视觉检查。
6. 完整本地真实gateway/mock推理链、全部尺寸/主题/辅助/E2E与最终发布门禁；最后统一部署Oracle现有域名。

所有未完成项仍在原计划范围内，没有移出本轮。生产账号、管理员、请求和账本均未改动。

## 后续执行上下文

执行清单：`docs/handoffs/prism-complete-alignment-execution.md`；设计规范：`docs/design/prism-compact-workspace.md`。
EgoLite继续复用空间16，不新建空间；本地5179是Vite演示数据预览，不能作为真实gateway验收。
局部截图和只读生产结构记录位于`output/prism-complete-alignment-20260913/`。
该目录的请求终态原型补丁未应用：必须先解决实际发送边界，再考虑接入，不能直接用于发布。
