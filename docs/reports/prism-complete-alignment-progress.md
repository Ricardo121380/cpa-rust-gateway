# CPAR 完整对齐实施进度

2026-09-13。本文件是阶段记录，不是最终交付报告；完整计划尚未完成；用户随后授权已完成批次先行上线，现已部署8578e55/schema27。下方批次的“未部署”描述保留当时状态；当前发布状态见 `prism-workflows-production-20260913.md`。

## 已提交

- `280302d`：持久请求终态、真实趋势与延迟、请求查询/导出及直接按模型编辑密钥。
- `fa84a2b`：普通/原生统一账号全量身份搜索、状态/类别筛选、排序与一致分页。

- `7e8dfc7`：按账号刷新真实上游目录；兼容目录解析与有界分页；目录总数；禁止发现模型自动开放与继承权限；旧空实现明确返回不支持；P12 空格变体展示修正；原生账号实际协议；密钥全选当前开放模型。
- `c19c91c`：提供商紧凑列表、独立接口/账号工作区、统一手机卡片与可见操作；高级调度折叠。

新增权威接口为 `POST /admin/catalog/refresh`，只提交接口/账号 ID，不提交 Client Key 或 Provider secret。真实生产目录核对尚未完成，不能把接口实现当作真实渠道验收结果。

## 本次验证

- 前端288项单测、类型检查、构建及131操作的四文件双构建门禁通过。
- 路由179项、HTTP库71项、目录/管理资源21项回归通过；gateway132项与组件冒烟1项通过。
- EgoLite 空间16：隔离演示数据下走通提供商创建与详情展开，检查1440×900浅色、1280×720深色、390×844手机。手机详情操作不超出视口。这些是局部视觉证据，不是全部栏目或真实授权验收。
- Oracle只读核对：服务仍active；活动配置仍为`production-models-20260913`、`rev-12`，四个配置模型及四个提供商。生产名称确实仍含P12；未执行生产迁移、别名删除或发布。

## 必须继续完成

1. 统一账号投影/全量搜索排序分页已实现；套餐筛选、完整详情、渠道首次授权与额度流程仍待完成。
2. Grok Build/Krill真实完整目录核对，按账号隔离、跨页选择与批量部分失败。
3. 固化线上实际服务与授权清单，生产副本演练正式命名和六个旧别名删除，验证回滚。
4. 请求观测已实现并完成本地受控链路；继续补全部页面的筛选联动和视觉验收，生产历史兼容/回滚演练尚未完成。
5. 剩余栏目统一操作、未保存保护、价格工作流和全站视觉检查。
6. 完整本地真实gateway/mock推理链、全部尺寸/主题/辅助/E2E与最终发布门禁；最后统一部署Oracle现有域名。

所有未完成项仍在原计划范围内，没有移出本轮。生产账号、管理员、请求和账本均未改动。

## 后续执行上下文

执行清单：`docs/handoffs/prism-complete-alignment-execution.md`；设计规范：`docs/design/prism-compact-workspace.md`。
EgoLite继续复用空间16，不新建空间；本地5179是Vite演示数据预览，不能作为真实gateway验收。
局部截图和只读生产结构记录位于`output/prism-complete-alignment-20260913/`。
该目录的旧请求终态原型补丁已被正式实现替代，不可再次应用。


## 请求观测与密钥编辑批次（本地，未部署）

- 新增 schema27 `request_finished` 必需事件；JSON、SSE、WebSocket 和 compaction 的终态由传输交付 guard 负责。编码错误记失败，提前释放记取消，不以源流结束直接判成功。首内容只在实际内容交给服务器传输层时观测，不能证明远端客户端已经收到。
- 管理鉴权下新增 `/admin/requests`、`/admin/requests/summary`；请求、终态、attempt、用量和账本关联，时间筛选下推 SQLite，事件和账本双 snapshot 绑定分页。历史缺失终态/时延保留未知。计数范围是已接纳的推理请求，不是所有 HTTP 流量。
- 仪表盘真实趋势、成功率、P50/P95、首内容延迟；请求列表筛选、详情重试链、脱敏已加载记录导出；时间窗口链接到用量/账本。没有账本不显示为零费用，未定价保持 unpriced。
- 密钥按名称及模型编辑，保留有效期精度、状态和额度。修改使用独立权限组以免改变其他密钥；已有模型保留原来精确的来源授权。共用额度组禁止静默拆分，提示高级维护。高级组操作折叠，普通列表以名称为主。
- EgoLite 空间16/本地真实网关：目录分页3模型，手动开放第二模型，UI创建受限密钥，旧密钥权限不变，越权404；缺价格模型的成功请求进入 unpriced 账本。UI再次编辑密钥后，移除模型返回404，其他密钥仍不变。真实 Provider 推理调用为0。
- 最新受控5请求为2成功、2失败、1取消，5 attempts，成功2条物化账本；包括SSE截断和显式TCP RST。普通TCP FIN不等于服务器可观测取消，不宣称远端收包保证。
- 大样本回归在100001对旧请求/终态外查询窄窗，结果完整。schema27拒绝丢失终态的降级；最终生产副本回滚方案仍待演练，不能直接替换schema26二进制。

本批复现入口：`scripts/acceptance/README.md`。检查日志与截图在 `output/prism-complete-alignment-20260913/`；私密凭据仅在 owner-only 临时目录。账号统一投影/全量搜索、渠道完整授权、真实目录、生产迁移、其余栏目及最终发布仍未完成。


## 统一账号查询批次（本地，未部署）

新增 `GET /admin/accounts/inventory`，安全身份搜索覆盖完整普通/原生目录；类别、状态、提供商、名称/渠道排序和游标分页统一。游标绑定配置/资源审计/原生代际，完整查询有10000条上限，超过报错而非给出不完整总数。目录查询不再次访问Provider。页面搜索300ms合并输入，显示完整匹配数，加载更多仅控制展示，不控制搜索范围。

本批验证：HTTP两项新增回归覆盖250条目录的末页邮箱、排序/筛选、秘密排除、旧配置游标、原生SSO身份导入与代际冲突；前端288单测/类型、HTTP全target clippy、134操作四文件门禁通过。EgoLite实际嵌入网关手机390×844走通搜索空结果、清空恢复、状态筛选，页面及行操作没有横向溢出。该服务只有5个合成API账号；不能据此宣称所有真实渠道授权已验收。

Oracle只读服务仍active，活动配置仍production-models-20260913：4配置模型、6遗留别名、4有效权限上下文模型数1/4/4/4。完整权限基线留在owner-only本地收据中，正式迁移前仍须重新核对。零生产修改、零真实推理。


## 价格导入确认批次（本地，未部署）

- 导入前展示相对所选目录的新增/变价/新目录不包含项，六类费率逐项对比；同模型不同提供商/渠道保持独立，显式零不丢失。已发布配置可以直接导入，内部自动准备并应用配置；全局目录已保存但应用失败时禁止重复提交并说明事实。
- 输入生效时间明确为本地时区；预览/持久时间带UTC标记。内部新目录ID自动生成；高级路由价格策略折叠。手机价目表使用卡片，详情/复制编辑/恢复价格直接可见。
- EgoLite16真实本地网关：两个受控未来目录分别完成预览（新增1/变价1）、确认、完成提示与重读，目录总数由1增至3，原价目表保留；未来时间保持未生效。仅合成数据，未触碰生产。手机390×844无横向溢出。
- 价格差异22项针对性测试（新增2项），前端总计290项、类型和134操作四文件门禁通过。自动远程价格源同步、所有字段的普通表格编辑、其余栏目/授权/生产迁移及完整最终验收仍未完成。
