# Prism 候选整体验收 — 2026-09-21

## 当前结论

候选基线 `8593447` 的真实本地网关闭环通过；本批另修复账号视图重复选择导致筛选丢失的问题。本地及远端正式门禁、双架构签名构建均通过；候选 `ca75a44` 已于2026-09-22部署 Oracle。发布与公网 EgoLite 检查见[上线报告](prism-candidate-production-20260922.md)。

## 本批修复

- `AccountRuntimePanel` 所在的账号视图导航：点击已经选中的“全部账号”或“运行状态”不再发出清空 query 的导航，保留当前搜索与筛选。回归同时覆盖两个视图。
- 备份 HTTP 测试临时目录增加原子序号，避免同进程并发用例取得相同墙钟时间而发生 `AlreadyExists`。备份生产逻辑未变，两个针对性用例通过。
- 两个价格目录测试将页面时间固定为 fixture 服务时钟；原先固定的未来日期在本次运行时已经到期，不能继续用真实当前时间测试未生效目录。
- 绑定核对测试跟随现有用户文案，在对应表格行核对缺失运行观测，仍检查不可解析绑定警告。
- 玻璃测试等待实际位移图生成后再检查 PNG 与尺寸，不用增加固定睡眠掩盖异步挂载。

## 真实网关与 EgoLite

新建合成管理员、隔离 SQLite、进程专用 CA、loopback TLS mock。未使用生产凭据或真实 Provider 推理。正式嵌入前端通过真实管理接口操作，不使用前端 fixture。

- 完成初始管理员登录及首次改密。
- 两页目录返回三个 exact ID；刷新后旧客户端仍只可使用已开放的一个模型。
- 界面选择 Model-Second，确认接入并应用。
- 界面创建密钥：全选实际选中两个已开放模型，再清空，只选择 Model-Second；签发后秘密仅写入0600合成临时文件，完成操作后 DOM 已清除。
- 旧密钥权限不变，新密钥只见 Model-Second；越权模型404，所选模型200，真实事件物化为缺价账本，金额保持 null。
- JSON/SSE 成功、上游503、流式截断、TCP取消共五个外部请求：两成功、两失败、一取消，attempts独立计数，两条成功记录完成物化。
- 真实会话撤销 immediate、settled、held 三种时序均通过：204撤销，受保护读取拒绝，可见登录页，无管理内容与JS异常。此前空画布仍未归因，本次未复现不等于已修复。
- 14个入口 × 1440×900、1280×720、390×844 × 深色/浅色，共84次结构观测，无页面异常或文档横向溢出；浅色组同时降低透明度、减少动效。此项是结构观测，不冒充每个页面全部业务动作通过。
- 设置外观通过聚焦后的 Space 键实际切换；辅助偏好令导航 backdrop-filter 为 none。
- EgoLite一次 full_page snapshot 挂起，页面 URL 与 DOM 查询正常；终止了该次只读 CLI，复用同一空间继续。没有切换 Safari。早期自动化定位失败已按实际 DOM 修正，不计为应用失败或成功证据。

脱敏机器回执及截图见 [证据目录](evidence/prism-candidate-integration-20260921/)。这些浏览器证据对应修复账号标签前的嵌入构建；该局部修复已在最终嵌入构建复核，两个视图均保留搜索，资源 revision `2be9c06023713b03f58fe5c6`。

## 门禁记录

- 前端单测：49文件、393项通过。
- 初次 fast：备份测试目录冲突失败；修复后该测试2/2通过。
- 第二次 fast：流式短总超时测试返回 EgressUnavailable；单独运行通过，未据此宣称全门禁通过。最终串行全门禁中该用例通过；初次失败保留，不改写为通过。
- 首轮完整 Chromium fixture E2E：293通过、5失败，零自动重试；与上述真实 EgoLite 验收分开计证。
- 供应链检查通过：固定工具版本、依赖策略、RustSec。
- 第二轮完整 Chromium fixture E2E：298通过、1失败；余下失败是绑定提示断言定位到名称 span，而非其所属表格行。两次修正分别处理名称节点和 has 定位的相对作用域，最终整个 batch-d 文件6/6通过。各次运行不累计为一次全绿；最终 fast 通过：1,282 Rust测试通过、9忽略；Clippy、四文件嵌入、契约、文档、边界、秘密扫描通过。回执见证据目录。

## 发布准备与收敛

切换前只读 Oracle 检查：原有 `b518d925fdaf03c92d235fa6ca4a7c5565d04043` active，loopback健康检查通过。当前候选相对此版本无后端生产代码或迁移修改；随后已完成精确提交的正式门禁、签名、断网生产副本回滚演练和切换回读。保留现有管理员、账号、历史和最新轮换凭据；不改 DNS/Caddy/Autoreg，不恢复旧数据库。

精确候选提交：`ca75a44351374aac62a40b13ba878a3487345db6`。远端[正式门禁](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/35621951153)及[签名构建](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/35621976728)均通过，正式切换及公网复核见本报告顶部链接。本次合成网关控制器与mock已关闭，未停止其他本地服务。
