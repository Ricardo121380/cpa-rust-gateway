# V2 价格与资源弹窗续验

2026-09-22，基线 `e3ce6cd`。本地正式网关、合成状态目录，未上线。

## 修改

- `CatalogRestoreDialog.tsx`：恢复来源采用真实模型、条目数、来源及生效/创建时间摘要；新目录与结果中的技术 ID 折叠，保留查询线索。
- `CatalogInspector.tsx`：目录模型取代主信息区中的随机版本编号，技术标识按需展开。
- 原始模型 ID 保持原样；目录复制、计价、配置与账本逻辑不变。

## 本次验证

- TypeScript、gateway 嵌入构建通过。
- 计费模型与任务回归 2 文件、35 项通过。
- SPA 152 个生成操作、CSP、四文件及确定性双构建门禁通过。
- EgoLite 实际检查：模型接入手机表单；提供商编辑取消退出保留输入、确认放弃退出；价格恢复预览三尺寸（390×844、1280×720、1440×900）；恢复返回修改保留时间；取消触发未保存确认；价格详情深色桌面与浅色手机；配置复制入口深色手机。
- 实际查看截图：模型接入手机、价格恢复确认手机、价格详情深色桌面/浅色手机、配置复制入口手机。其余两张恢复确认截图保留，不记为人工图像审阅。
- 价格时间输入通过 EgoLite DOM 原生 value setter 和 input/change 事件设置；CLI fill 没有正确设置 datetime-local，未将此工具限制记为产品故障。
- 没有提交新目录、创建草稿、改变生产状态或调用真实 Provider。

证据：[assets/prism-v2-price-dialogs-20260922](assets/prism-v2-price-dialogs-20260922/)。

## 当前限制

本次没有全量复测所有弹窗。模型别名/删除/高级候选、价格导入最终预览、配置应用/回滚确认等仍沿用前批证据，需统一最终视觉复核。未宣称 V2 全部复刻完成。

检查期间手动输入不存在的 `#/config-versions` 会显示 Router 默认 404；正确内置链接是 `#/versions`。已从设置页恢复，本轮未修改路由错误页。

## 同轮继续核对

- 模型别名新增表单与模型删除确认的浅色手机截图已实际查看。别名输入 `TemporaryAlias` 后 Escape 触发未保存确认；选择继续编辑保留输入，随后确认放弃退出。没有创建别名或删除模型。
- 接续既有本地草稿后，检查高级候选新增工作区及滚动到表单底部的操作按钮，均截图并实际查看；没有创建候选。已发布配置下禁用高级写操作的行为也得到验证。
- 现有草稿来自较早活动配置，校验应用被拒绝，提示“此草稿来自较早的已发布配置，请重新核对来源”。拒绝界面截图已实际查看。未执行应用；该结果仅覆盖失败态，不能算成功确认/提交验收。
- 此增补更新前文限制：模型别名/模型删除与高级候选新增的手机外观已查看；其他尺寸/主题、候选编辑/删除、配置成功应用/回滚确认和价格导入最终预览仍未在本轮完整重验。
