# 近期前端改动整合核对 — 2026-09-21

## 结论

候选代码 ce70125 的账号、提供商、目录与模型工作区定向整合检查通过。本批只补验和记录，没有产品代码改动，没有上线。不能据此宣称八个工作区全面对齐或最终发布验收完成。

## 本次证据

| 路径 | 验证方式 | 结果 |
|---|---|---|
| 账号操作、资源 CRUD、来源编辑、目录快照、模型筛选、提供商详情 | Chromium 六个 spec，59/59 | 通过 |
| Kimi/Kiro 设备授权轮询、取消、返回及授权/导入拆分 | Chromium channel-authorization，8/8；模拟服务 | 通过，不代表官方授权完成 |
| 筛选代次清理、资源名称与子资源投影 | Vitest 三文件，22/22 | 通过 |
| 缺账号/缺接口 | EgoLite，真实本地 gateway55370，合成库 | 两种提示正确，保存按钮均 disabled |
| 提供商→目录→已接入模型→目录 | 同一真实网关会话 | provider=local-provider、endpoint=local-responses、account=local-account、search=exact 保留 |
| Kimi Coding 授权/导入切换 | 真实管理接口读取渠道能力；未发起外部授权 | 授权态不出现提供商/接口选择；切换后合成凭据输入清空；取消仍经过未保存确认 |

浏览器中创建了一个无凭据的本地合成提供商草稿，用于缺账号检查；随后删除本次新建的接口以检查缺接口分支。草稿未发布，生产资源和历史数据未修改。浏览器任务已关闭。

日志：`/tmp/prism-integration-e2e.log`、`/tmp/prism-integration-channel.log`、`/tmp/prism-integration-unit.log`。59与8是本批两个不重叠测试集合，共67条浏览器回归；不叠加前几批重复执行数量。

空态验收依据为真实页面语义快照和按钮 disabled 属性。截图捕获了弹窗入场过程，不能作为稳定视觉证据，已从交付中移除。自动化过程中两次选择器使用错误（href与Kimi旧值）经当前页面快照纠正，不归为产品失败。

## 发布差异核对

以本地已记录部署基线 b518d92 对比 ce70125：五个产品提交 d651680、36fb7d4、85d5754、13cef76、ce70125，涉及授权/导入主次、目录来源和筛选连续性、提供商详情及资源表单。没有 apps、crates、权威OpenAPI、生成客户端或数据库迁移差异。此处是 Git 与已有部署记录对照，本批没有连接远端，不能视为线上版本实时证明。

最终候选在前一批已通过构建、四文件、确定性双构建及3条Rust嵌入检查；本批代码未变，没有重复构建。后续正式发布仍需复核远端实际版本和签名发布门禁。

## 剩余与下一步

1. 完成 API 密钥、请求日志、用量费用和设置工作区的本轮流程复核；本批没有重新验收这些页面。
2. 将模型手动开放→受限密钥→本地mock请求串成实际新候选端到端验收，区分模拟推理与真实渠道目录/授权。
3. 冻结候选后检查 Safari/Chromium 已有标签页跨版本刷新，再执行正式发布门禁和Oracle部署；历史Safari热修复证据不替代新候选复验。
4. 全站参考项目逐操作映射及多资源视觉对照仍开放；不以本次67条通过替代缺失证据。

下一开发批优先核对模型开放与 API 密钥授权衔接，以新模型默认不扩权、密钥只授予显式选择范围为验收标准。
