# Prism Liquid V2 本地迁移记录

状态：V2 本地视觉候选已实现；共享层定向验收已运行，生产未部署。基线 `de888dd`；实现提交 `51343ce`。用户提供的 V2 原型已存入 `docs/design/chatgpt-prism-liquid-v2/`。

## 已实施

- 品牌移入全高侧栏，顶栏仅覆盖内容区；真实会话名称、工作区搜索；保留八主入口及高级路由。
- 取消共享磨砂内容外壳，改为统一实底数据面板；统一渐变主按钮、珠光次按钮、图标/文字与危险操作。
- 统一居中 Sheet、右侧详情和页内编辑的材质、标题、字段、底栏；沿用原有业务生命周期。
- 总览四指标、请求趋势与关注事项双栏，真实时间桶支持请求量/平均耗时切换，缺失时延断线。
- 账号页标题后显示视图导航，分类选择和列表保持原有身份、来源、协议和能力。

## 当前证据

- 类型检查通过；前端单元 49 文件、393 用例通过（本轮运行）。
- `npm --prefix web/prism run check` 通过；生产 build 输出约定四文件，保留 main 超 500 kB 提示。
- EgoLite TaskSpace 9：八工作区 × 1440×900、1280×720、390×844 × 浅/深色，共 48 个布局观察。无观察到的整页横向溢出或错误边界崩溃。此项不是全量功能验收。
- EgoLite：工作区搜索→账号；Kimi Coding 官方授权/导入切换；未保存输入 Escape 提示、继续编辑保留、放弃后重开清空。
- 390×844 深色辅助降级：Sheet animation none，motion-panel 0ms，topbar blur none；弹窗 x=12、右界378，底栏按钮44px。
- 持久证据：`assets/prism-liquid-v2-20260922/`，包含真实网关 48 张深浅色页面截图、布局 JSON、fixture 深浅色布局、代表性弹窗截图、测试日志与四文件哈希。

## 最终候选补验

- `check:full` 双构建一致性通过；四个 served 文件逐字节匹配 dist；嵌入资源 Rust 测试 3/3。
- EgoLite 真实隔离网关：8 工作区 × 3 尺寸 × 深浅色，48 个观察无整页横向溢出或错误边界。使用合成资源，无推理请求；旧标签页跨构建重新加载后正常登录。
- 搜索输入初始焦点由 Sheet 统一管理，关闭回顶栏搜索；dirty 编辑器取消离开保留输入，确认放弃后搜索立即可输入；系统及应用内减少动态 × 深浅色 × 主/次按钮共 8 组，hover 与按住状态 transform 均为 none（`motion-receipt.json`）。
- fixture 访问组写入保持 pending 时：关闭禁用、Escape 不退出、Ctrl+K 不叠加搜索，释放后展示草稿回执。
- fixture 一次性密钥展示后注入认证拒绝：回登录页，弹窗/秘密节点/查询缓存均清空，session 无密钥。
- 账号分类总数来自分页前 category_totals；配置凭据与独立原生账号分别说明作用域。运行绑定来自独立实时接口，上限 100，有 cursor 时显示部分；绑定数不冒充唯一账号数。
- 请求和费用共用时间范围，账本查询共用 hook；真实空账本保留未知，费用不显示为零。

## 明确限制

- C2C iteration 2：`APPROVED_FOR_LOCAL_PREVIEW`，R1–R3 CLOSED。结论仅覆盖当前视觉候选与三个修正，不代表全计划、全业务或生产就绪。复审之后补充了真实网关浅色 24 项与按住状态 8 组证据；未改变产品代码。
- 审阅记录：[ChatGPT 设计与复审会话](https://chatgpt.com/g/g-p-6aa8d7134f2c8191a372d21f415c3e83/c/6ab1479d-31a8-83ea-aa5e-fa4f3df3bd8a)。日志以 `*-final.txt` 纳入版本库。
- 本轮为现有功能的视觉迁移；未重新跑所有业务端到端场景，也未把 V2A–V2E 的完整行为矩阵标为完成。
- 原型的演示健康数、模拟目录、假差异前后值不迁入；复杂提供商/路由/价格编辑沿用页内工作区。
- 未执行 Safari/WebKit，未新增真实 Provider 推理和生产操作；历史会话空画布未宣称修复。

## 查看方式

- EgoLite 已登录的本地隔离实例：http://127.0.0.1:53438/admin-ui/#/ 。该入口依赖本机验收进程，使用合成资源。
- 静态截图：`assets/prism-liquid-v2-20260922/1440-overview-light.png`、`1440-overview.png`、`390-key-form-dark.png`。
- 生产域名未切换到本次候选；管理员、历史请求与账本均未改动。
