# V2 配置生命周期与价格预览验收

2026-09-22，基线 `1cb1ccf`；本轮只操作 `127.0.0.1:62992` 隔离合成网关，未部署生产或调用真实 Provider。

## 已完成

- 从当前活动配置复制新草稿 `V2 local lifecycle acceptance`，通过完整差异及配置校验后确认应用；点击“核对服务端状态”，返回“配置应用已确认”“目标仍为当前活动配置”。
- 确认回滚至本轮之前的“更新模型来源 · Exact/Model-v2”，重读返回“配置回滚已确认”“目标仍为当前活动配置”。本轮新配置成为历史，未删除原有草稿或历史数据。
- 发现成功应用后旧 review 参数导致误报“此配置不再是草稿”。`VersionsPage.tsx` 对服务端已确认 active 的目标显示已发布状态及关闭变更入口，其他已归档/缺失目标仍保持警示。最终构建验证 active review 显示正确状态、无旧错误，关闭入口正常。
- `CatalogImportDialog.tsx` 对比选项和预览基线改用原始模型及生效时间；新目录技术 ID 收入折叠区。原始 ID 与写入值不变。
- 复制已有价目表，填入本地生效时间，进入完整预览（1 条、0 差异）；返回修改保留时间，取消经放弃提示退出。未确认导入目录。

## 证据与验证

- EgoLite，实际正式前端与本地真实 gateway。
- 应用确认、价格预览各保存 1440×900、1280×720、390×844 截图；回滚确认保存手机截图。
- 已实际查看：应用确认手机、回滚确认手机、价格预览手机/桌面。截图范围不等于全部主题和全部状态签收。
- 应用/回滚回执见 `assets/prism-v2-lifecycle-20260922/*-receipt.txt`。回滚截图背景中的旧错误提示是修复前证据，不代表最终构建。
- 最终 TypeScript、gateway 嵌入构建通过；配置生命周期和计费任务回归 2 文件 / 21 项通过；152 操作、CSP、四文件、确定性双构建门禁通过。
- 本轮复制配置带来 6 条密文存储记录差异；界面如实标识不能从密文比较推断秘密明文变化。

## 交付边界

本轮补齐之前缺失的本地成功应用/回滚路径及最终价格预览，未以此宣称全站像素级复刻。剩余完整主题/尺寸矩阵、候选编辑/删除及用户视觉验收仍需收敛。生产未发布。
