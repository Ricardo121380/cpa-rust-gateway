# 设置、会话与构建切换核对 — 2026-09-21

## 结论

本批未修改产品逻辑，新增闲置到期的浏览器回归，并核对设置页真实网关行为。正常退出、服务端撤销后重读、重新登录、刷新清空会话均有实际证据。EgoLite 同一标签页跨构建资源切换通过；这不是正式部署验收，也不能代替 Safari 验收。

基线为 `42a8bcc`，真实嵌入 gateway 使用该批对应产品构建，管理监听为 loopback 55380，沿用隔离合成状态；无生产写入、真实 Provider 推理或账号授权。

## 代码与测试

- `web/prism/src/session/sessionStore.ts`：会话替换/锁定增加 generation、重置配置；绝对到期定时器按 generation 检查当前归属。
- `web/prism/src/api/client.ts`：会话切换取消旧请求；请求和响应体解码检查归属；退出立即本地锁定，另用捕获的旧会话完成有界撤销。
- `web/prism/src/api/queryClient.ts`：会话 generation 改变同步清除 query/mutation 缓存。
- 新增 `web/prism/e2e/session-ownership.spec.ts` 的闲置到期场景：合成短会话下打开密钥编辑器，推进浏览器时钟到期，断言登录页、编辑器消失、秘密及两类缓存清空。此项是 fixture/时钟测试，不声称真实等待八小时。

本次执行结果：

| 检查 | 结果 |
|---|---|
| settings + session-ownership Chromium（最终） | 6/6；无重试 |
| client.ownership 单元回归 | 22/22；覆盖旧 session/版本晚到、解码晚到、登录归属、退出及绝对过期 |
| sync-contract | 权威契约无变化，生成物无差异 |
| check、TypeScript | 通过 |
| diff whitespace | 通过 |

修改前5项与最终6项存在重叠，不相加。没有产品代码变动，因此未重复上一批已通过的 Rust/四文件构建；正式发布仍需最终候选的统一门禁。

## 真实网关 / EgoLite

- 设置页选择浅色、减少透明度、增强对比度、减少动效，根节点分别出现对应已定义属性。
- 点击退出后进入登录页，再访问设置深链仍返回登录页，管理画布不在 DOM。
- 通过页面内存捕获该合成登录的会话，只用于调用真实 `/admin/auth/logout`；撤销返回204。点击设置页刷新后返回登录页，无管理内容；捕获信息没有写出日志或文件。
- 重新登录后可打开设置；整页刷新后回到登录页，密码框为空。
- 早期撤销验证的自动 URL 跳转路径曾出现空画布/等待超时，未取得错误事件，不能将该次记为通过。后续采用实际设置入口与刷新按钮的完整路径通过。自动导航与撤销竞态仍应在最终发布检查中复核，不能据本批结论排除所有导航竞态。

## 同一标签页跨构建资源检查

临时 loopback 55480 只服务实际构建的四个文件，不实现或模拟管理 API。旧产物由 `af89c2c` 的前端源码构建，新产物取当前已验证 dist。静态资源允许缓存一小时，HTML 不缓存；同一 EgoLite 标签页加载旧版后切换服务目录并正常刷新，未清缓存。

- 旧 revision：`7fd29ab79943be05b928faea`
- 新 revision：`c628bc48d1c4633777b6fdfb`
- 新 main/vendor/CSS 均使用新 revision；transferSize 分别817583/141974/122283，均重新传输。
- 刷新后管理员登录表单正常出现。

这验证资源 URL 隔离与加载，不证明新旧后端切换、已登录会话跨服务重启或真实生产缓存链路。临时静态服务和本批浏览器空间已结束。

## 剩余与下一步

优先复核撤销会话与导航并发时的空画布现象，确认是自动化导航还是应用竞态并补可靠用例；随后完成 Safari 同标签页切换与最终发布候选检查。全站参考行为矩阵、视觉差距与最终发布仍按总台账推进，不将本批标为整体计划完成。
