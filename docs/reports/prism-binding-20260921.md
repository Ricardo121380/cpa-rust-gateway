# 账号连接表单 — 2026-09-21

## 本批实现

- 将接口和账号归为连接对象；启用状态从折叠调度参数中移到主表单。
- 优先级、权重、并发上限桌面三列、手机单列，保留原默认值、范围及请求体。
- 没有接口或账号时提示先创建对应资源并禁用保存；必填选择仍由表单校验。
- 核对连接面板的表头、空态及未观测标签中文化。未改变配置与运行观测的来源。

## 验证

TypeScript通过，资源操作浏览器回归19/19通过；gateway构建、四文件/确定性双构建、Rust嵌入3/3通过。日志为 `/tmp/prism-binding-{e2e,build,gate,embedded}.log`。

EgoLite使用真实嵌入gateway55370，同一隔离合成数据副本；接口行入口正确预选local-responses，默认启用，展开调度参数后在1440×900、1280×720、390×844无横向溢出。选择local-account后Escape触发放弃确认，继续编辑保留选择，明确放弃后关闭并回到连接账号按钮。没有提交连接或调用真实上游。浏览器任务已关闭。

[桌面修改前](evidence/prism-binding-20260921/before-1440.png) / [桌面修改后](evidence/prism-binding-20260921/after-1440.png) / [1280](evidence/prism-binding-20260921/after-1280.png) / [手机](evidence/prism-binding-20260921/after-390.png)。截图中的无身份账号是合成样本，不是生产身份验收。

本批未上线；空资源提示仅经过静态检查，尚未单独做真实网关空资源场景。下一步汇总近期授权、目录、提供商与编辑表单变更，进行跨工作区流程验证及发布差异核对，不以局部回归代表全站完成。
