# 账号身份一致性修复上线记录（2026-09-12）

按用户“上线让我人工验收”的明确授权，已将 `5b92e15443bd0c2e3a8c44c5d9264999a2066a3e`
部署到 Oracle 新加坡既有 CPAR 服务，替换 `a243aab`。正式入口：
[Prism 管理端](https://cpar.142857142.xyz/admin-ui/#/accounts)。原管理员账号与密码继续使用。
应用改动及本地功能测试详见 [修复报告](prism-account-identity-continuity-20260912.md)。

## 已上线与人工验收范围

- 全部账号：相同渠道、相同邮箱/电话归为一个身份；Codex 的两条相同授权记录显示为一行，
  “管理授权”保留两份记录的精确操作。没有删除或停用底层凭据及绑定。
- 运行状态：使用实际运行配置提供的人类身份、真实渠道及协议/主机；不再以旧阶段 ID、
  “Codex 桥接 上游”作为主标签。每条运行连接继续独立展示认证、调度和并发。
- SSO：新导入会自动尝试查询身份；已有缺邮箱账号提供“读取身份”，失败不会撤销有效导入。
  身份以新 schema 25 观察表加密保存，凭据和观察 CAS 防止晚响应污染。

上线后只读查询：3 份普通授权中 2 份有身份，9 条运行连接均返回新的展示元数据；
5 个 Grok 账号中，3 个 Build 有 2 个邮箱，2 个 Console 均无邮箱。这些是身份观察数量，
不是授权可调用数或渠道健康结论。上一轮 Console 资料请求的 403/Cloudflare 限制尚未
实测解除；本次发布没有额外触发资料请求、刷新、授权或推理 canary。

EgoLite 已刷新正式 HTTPS 登录入口并留给用户继续操作。此次生产浏览器验收覆盖登录页，
没有读取或代填管理员密码；登录后的功能由上一轮真实本地 EgoLite 操作，以及本次生产鉴权
API 读回分别佐证。用户可以重点核对账号归组、各份授权详情和运行状态；如已有旧页面，刷新后
使用原密码重新登录即可。

## 发布验证与保留的数据

| 检查 | 结果 |
| --- | --- |
| [签名构建](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/34687887227) | Linux ARM64、x86_64 均通过 |
| [正式发布检查](https://github.com/Ricardo121380/cpa-rust-gateway/actions/runs/34687888277) | 同一提交的 Fast 与供应链检查全部通过 |
| 独立制品校验 | revision、manifest、SBOM、receipt、Cosign 身份及签名通过 |
| ARM64 合成状态 | 实际管理员初始化/首次改密、Origin/CSRF、注销、重启及四文件哈希通过 |
| 生产副本演练 | 无外部网络、服务用户运行；24→25→24，保存加密合成身份并实际读回，旧版重启通过 |
| 正式切换 | 停服至就绪 1236 ms；当前进程 SHA256 与签名包一致 |
| 公网读回 | 服务器与操作者网络的四文件哈希均一致；health 通过，匿名管理访问被拒绝，CSP 保留 |
| 浏览器 | EgoLite 正式登录页正常，已保留给人工验收 |

实际进程 SHA256：`b3d239b2538bf5f2ec7741002c32e074f82bea81622ddc9d2e2d68321ac28e96`。
发布前已有 2229 条事件、593 条账本、87 条未解决物化记录及 11 条测试草稿退役审计保留；
当前生效配置、所有原生账号与管理员库保留。未变更 Caddy、DNS、Autoreg 或 loopback 监听。
没有把本地合成账号、演练身份或配置写入生产。服务原有后台任务继续按既有规则运行。

## 备份与回滚

服务器私有发布目录：`/var/backups/cpa-rust-gateway/prism-identity-continuity-20260912`。
切换前复制服务凭据，停服务后用 SQLite backup 保存一致控制库及管理员库；旧 release 保留。
回滚脚本及本轮制品、验签、演练记录在该目录。本地编排在
`output/prism-identity-continuity-release-20260912/`，脚本哈希进入脱敏证据。

回退到 `a243aab` 必须先停服务并备份当时最新数据库，再使用本轮 `migration_rollback.py`
移除仅新增的 schema 25 表/触发器及对应迁移记录，然后切换旧版启动。脚本核对所有既有数据表
指纹，拒绝未知 schema，支持重复执行；回滚前快照保留新身份观察。不会用发布前的旧库覆盖
最新账本或轮换令牌。`a243aab` 已支持 compact v2，本次回退不转换凭据格式。

上述回退已在隔离副本实际执行并启动旧版，**生产未执行回退**。发布脚本在失败时使用这一
已演练路径；成功后不会主动降级。回退不能只换二进制。

证据：[脱敏发布记录](evidence/prism-identity-continuity-production-20260912.json)、
[EgoLite 正式登录页](evidence/prism-identity-continuity-production-20260912.png)。
身份查询的值与所有凭据均未写入报告或提交。
