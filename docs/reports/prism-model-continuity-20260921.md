# 模型工作区筛选连续性 — 2026-09-21

## 本批结果

“上游模型”与“已接入模型”切换后保留各自读取偏好：目录的提供商、接口、账号、搜索词，以及已接入模型的独立搜索词。只使用会话内 Zustand 状态，不新增浏览器持久化。

配置选择或会话代次变化清空缓存；旧组件的 effect 不得在代次变化后重新写回。显式目录深链在页面初始化时优先于记忆，并从空白目标组成新的选择，避免混入前一个提供商的接口/账号。显式URL参数仍需通过当前资源清单解析，缓存不是授权证据。

不保留模型批量勾选、目录快照、页码、刷新结果或未完成写入。原始模型ID、目录账号权限、CAS及接入回执未改动。本批未部署，也未调用真实上游刷新或推理。

## 验证

- TypeScript通过；新增筛选单测2/2通过，覆盖独立搜索、配置切换与锁定清空。
- 模型目录生命周期和新增工作区切换浏览器回归16/16通过；包含原有来源编辑、快照变更、分页失败及忙碌保护。日志 `/tmp/prism-continuity-e2e.log`。
- gateway构建、管理SPA门禁（含确定性双构建和四文件检查）、3条Rust嵌入测试通过。日志 `/tmp/prism-continuity-build.log`、`/tmp/prism-continuity-spa.log`、`/tmp/prism-continuity-embedded.log`。
- EgoLite：最终真实嵌入gateway `127.0.0.1:55340`，隔离合成数据库。选择 `local-provider / local-responses / local-account`，目录搜索 `exact`，切到已接入模型搜索 `local`，往返后四个目录值与独立模型搜索均保持。机器可读结果：[read-filters.json](evidence/prism-continuity-20260921/read-filters.json)。未提交资源修改；任务浏览器已关闭。

这次是行为连续性验证，不宣称全站视觉改造或真实渠道授权验收完成。下一批继续逐栏目主操作、详情和复杂编辑的结构统一，并补对应视觉差异证据。
