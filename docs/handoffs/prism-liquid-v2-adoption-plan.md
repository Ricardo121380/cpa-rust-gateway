# Prism Liquid V2 正式应用迁移

> 2026-09-26 状态索引：[当前清单](../reports/cpar-current-status-20260926.md)。本计划后续V2结构/精修已于9月24日生产发布；历史“未上线”条目是当时记录。最近生产登录态全工作区回归仍未补齐；用户已允许恢复EgoLite空间1，旧初始密码一次登录被拒绝，现等待用户在页面输入当前密码。

2026-09-22。来源：用户 ChatGPT 设计会话 `6ab1479d-31a8-83ea-aa5e-fa4f3df3bd8a`，C2C `c2c_v2adopt`，PLAN iteration 0。基线 `de888dd`，保留无关未跟踪文件。

视觉源：`docs/design/chatgpt-prism-liquid-v2/CPAR-Prism-Liquid-v2.html` 与 `C2C-Design-Handoff.md`。原型只有演示数据，不作为生产业务或验收证据。

## 批次与验收

|批次|实施内容|完成条件|
|---|---|---|
|V2A|全高悬浮侧栏、内容顶栏、实底面板、按钮、弹窗、动效与辅助降级|三尺寸深浅色、焦点、触控、无溢出、类型和构建|
|V2B|总览四指标、趋势/关注、账号概览、账号列表和授权入口|真实字段/范围、未知值、授权表单/关闭保护、分页和筛选保留|
|V2C|提供商、模型、密钥工作区与表单|保留拓扑、手动开放、显式权限与一次性秘密|
|V2D|请求、用量价格、设置和待应用核对|时间深链、六类 token、置信度、完整差异与显式应用|
|V2E|冻结候选整体本地验收|EgoLite、正式嵌入/CSP、四文件一致、报告和独立审阅|

## 页面映射

|工作区|正式入口|适配重点|
|---|---|---|
|总览|`#/`|四指标、双栏趋势/关注、费用与账号摘要|
|请求日志|`#/monitoring`|筛选、真实终态、详情及尝试链|
|用量与费用|`#/usage`、`#/billing`|范围与置信度、复杂价格编辑保留页内|
|账号|`#/accounts`|标题后导航、渠道筛选、统一列表、官方授权及导入|
|提供商|`#/upstreams`|紧凑主从工作区、页内新建、接口及绑定|
|模型|`#/models`、`#/catalog`|原始 ID、来源、开放与候选编辑|
|API 密钥|`#/access`|名称、允许模型、一次性结果与权限维护|
|设置|`#/settings`|外观/辅助/会话，维护入口保留旧路由|

## 不可变边界

- 复用 React/TanStack/Zustand、GlassSurface/PrismLens、Sheet/InlineWorkspace/OperationBoundary/ConfigurationLifecycleHost。
- 不复制原型内联事件、模拟任务、原生 dialog、假运行状态或前后差异。真实关闭和秘密清理不等待动画。
- 仅三面 chrome 玻璃；正文实底。不新增 v7 覆盖层、依赖或 chunk；统一客户端、CSP 和四文件构建不变。
- 请求与账本同时间范围、各自快照；授权计数不是可调度数，部分分页不作完整统计。
- 草稿保存不等于发布，全局价格独立保存，原生账号操作即时生效；不自动重放冲突写请求。
- 本轮本地复刻与验收，不做生产操作、真实授权、额度探测或推理。EgoLite 是浏览器验收入口；不静默改用 Safari。

## 验证记录规则

证据落入 `docs/reports/prism-liquid-v2-adoption-local.md`。区分原型、fixture、真实本地 gateway；不得以历史通过数替代本次结果。C2C 通过连接器读取 diff 和 execution output，不粘贴秘密或日志。

## 2026-09-22 第二轮结构对齐

用户指出第一版不足后，进一步完成紧凑提供商列表/右侧工作区、连续账号面板、主页面标题后的下划线页签、密钥优先、横向设置行，以及请求/用量的筛选层级。详见 `docs/reports/prism-liquid-v2-fidelity.md`。这是 V2B–V2D 的推进，不将页面存在或布局观察等同于 V2E 全部业务通过。仍保留原型与真实业务的差异清单，生产尚未发布本地候选。

## 2026-09-22 本地候选收尾

本轮按用户“连续开发直到验收”要求完成核心表单分组、密钥真实权限摘要、创建权限搜索和价格协议显示，并使用新的隔离真实网关完成接入/密钥/请求/账本/价格等链路。V2A–V2E 本轮本地交付及验收范围见 `docs/reports/prism-liquid-v2-local-acceptance.md`；该报告取代早期报告中密钥摘要等“仍未完成”状态。生产与真实官方授权仍不在本轮范围，未将布局通过当作全业务或像素级复刻认证。

## 2026-09-22 人工视觉复核后的修正

用户明确否定“功能验收即可视为完整复刻”。后续以 V2 原稿最终渲染为准，先逐页实现视觉结构，再适配真实功能；不得用原型假数据替换生产逻辑。本轮字体、材质、首页与渠道选择入口修正见 `docs/reports/prism-v2-type-material.md`。上一轮本地功能通过记录保留，但全站视觉复刻状态仍未完成。

## 2026-09-22 账号与资源列表结构批次

账号主操作/状态筛选/搜索工具分层、普通与原生账号身份详情头部、模型 ID 与来源同栏、提供商协议摘要已实现。最终本地真实 gateway 的三工作区 × 三尺寸 × 深浅主题检查及操作证据见 `docs/reports/prism-v2-account-structure.md`。全站复刻仍进行中：复杂维护/授权弹窗、密钥、请求、用量和设置需要继续逐项对照；未部署生产。

同批补齐请求详情分区与尝试时间线，已在真实合成请求上完成三个尺寸检查；请求列表与其余工作区仍需继续对齐。

## 2026-09-22 密钥、用量与会话批次

密钥详情增加完整真实模型权限分区；用量标题时间控件、三列摘要与手机六类 Token 卡片；会话设置统一横向行。详见 `docs/reports/prism-v2-operations-structure.md`。复杂授权/批量/多来源/价格与差异工作区仍需继续，未上线。

## 2026-09-22 剩余工作区与整体复核

补齐批量账号结果、凭据更新、Codex/Claude OAuth、Kimi/Kiro/Grok 设备授权、来源调度编辑、价格导入/恢复/策略、待应用与历史差异结构；另修正请求筛选宽度、密钥边距和工作区标题遮挡。此处更新的是 V2 剩余结构实施及本地验证状态，用户视觉验收仍以实际页面为准，不称像素级完全一致。

本轮证据、逐页映射、真实操作与未触发的官方授权边界见 `docs/reports/prism-v2-workflow-alignment.md`。11 入口 66 项布局检查，最终修正补查 18 项，前端/契约/四文件/真实网关嵌入检查通过。生产未发布。原型演示费用分摊不移植，用量保留真实六类 Token 和账本入口。

## 2026-09-22 用量费用双栏补齐

按 V2 原稿补齐请求数/成功率/P95/已知费用四指标、真实模型费用来源及计价完整性双栏，六类 Token 下移独立明细；请求与账本使用同时间范围并明确各自筛选限制。详见 `docs/reports/prism-v2-usage-alignment.md`。这替代上一条“用量仅保留 Token 与账本入口”的限制，不把单页六组布局检查推定为全站视觉复刻通过。仅本地，未上线。

## 2026-09-22 跨页视觉收敛

修复操作 td 被 flex 覆盖、模型/密钥内缩表格、设置重复边距，增加真实账号目录摘要，将请求统计改为可展开摘要。66 项布局观察与最终两页补查、37 项针对性测试通过。详见 `docs/reports/prism-v2-convergence.md`；后续缺口明确为账号主表额度/调度/观测、模型运行可用性列和完整弹窗视觉签收，不再用泛称“收尾”替代差异清单。未发布生产。

同轮继续补齐账号“运行与额度”列及模型“运行可用性”入口，复用真实 provider-account-pools / Explain，完整分页、快照一致和有界读取新增 3 测试通过。额外 12 项布局与模型到 Explain 真实只读链路通过。数值额度与通用可调度账号数不在现有接口中，未知如实呈现，不复制原型演示值。详见 `prism-v2-convergence.md` 同轮增补。

## 2026-09-22 弹窗续验

账号接入的渠道/方式切换保护与标题精简已完成，见 `prism-v2-import-dialogs.md`；价格恢复/详情的目录身份层级已统一，资源编辑及配置复制入口补验见 `prism-v2-price-dialogs.md`。尚待最终视觉复核：模型别名/删除/高级候选、价格导入最终预览、配置应用/回滚确认。保留此前功能证据，但不把本轮局部检查扩张为全部复刻完成；未上线。

同轮补验了模型别名新增、模型删除确认、高级候选新增及表单底部手机外观。既有旧基线草稿被正确拒绝应用，记录为失败态而非成功应用验收。最新具体覆盖以 `docs/reports/prism-v2-price-dialogs.md` 同轮增补为准。

## 2026-09-22 成功应用与回滚补验

新建当前基线本地草稿，实际完成应用、服务端重读、回滚与重读；补齐价格导入最终预览及返回保留输入。修正成功应用后的过期草稿错误提示和目录对比编号展示。详见 `docs/reports/prism-v2-lifecycle-acceptance.md`；取代此前成功应用/回滚和最终价格预览未验的状态。生产未发布，完整主题/尺寸与候选编辑/删除视觉覆盖仍需汇总。

## 2026-09-22 候选编辑与删除收敛

隔离真实 gateway 已完成候选修改/重读、删除/重读及最后来源删除后的拓扑失败验收。候选编辑精简身份层级、可读协议选项、双列调度字段，并修复桌面固定保存栏压住字段；最终三尺寸 × 深浅主题复核通过。见 `docs/reports/prism-v2-candidate-acceptance.md`。验收草稿未发布，生产未改。下一项为价格预览、应用/回滚复杂状态的剩余主题矩阵与全站差异汇总，不将局部通过标作全部复刻完成。

## 2026-09-23 复杂状态主题矩阵与状态收敛

价格完整预览、配置应用确认、回滚确认各补齐三尺寸 × 深浅主题，共 18 组；本日没有重复确认写入。新建隔离视觉验收草稿未发布，生产未改。八工作区状态、真实数据必要差异、官方授权中间态和视觉签收边界统一见 `docs/reports/prism-v2-final-visual-status.md`，不再将已验证的候选维护/确认弹窗矩阵列为未开发。

## 2026-09-23 预览恢复与账号密度

原预览进程/浏览器空间失效，取得用户允许后恢复为 EgoLite TaskSpace 1，沿用原本地状态。正文实际字体栈与 V2 一致；账号运行证据入口合入状态行以减少重复。六组最终稳定态、运行页链接、3 项回归与构建门禁通过，见 `docs/reports/prism-v2-density-review.md`。mock Provider 未恢复，本次不声称推理验收或生产发布。

## 2026-09-23 固定视觉差异收敛交付

以 `87675f8` 为基线，对八工作区原型/正式应用逐页对照后，完成 C1–C10：主从宽度与密度、分段控件、列表工具栏、趋势底栏、表格层级、开放表单分区及手机关闭目标。EgoLite 48 页面状态 + 42 弹窗状态、真实本地筛选/深链/未保存保护/受控读取错误恢复/辅助偏好已验证；最终四文件与服务返回逐字节一致。

报告 `docs/reports/prism-v2-closeout-20260923.md` 为本批代码/证据索引；`prism-v2-final-visual-status.md` 已同步。固定差异开发与本地验收完成，进入用户最终视觉签收；不将原型假值、未观测额度或真实官方授权中间态冒充完成，不涉及生产发布。此前分批待办以这里和最新报告的具体状态为准，历史记录仅用于追溯。

## 2026-09-23 Computer Use 复核与四项修复

后续 Computer Use 复核仍发现四项具体视觉/交互差异，详见 `docs/reports/prism-v2-computer-use-acceptance-20260923.md`。已按用户要求修复总览、提供商详情、手机账号密度和密钥模型空搜索，证据见 `docs/reports/prism-v2-computer-use-fixes-20260923.md`。这收敛四项问题，不将此前“固定差异完成”扩大为逐像素复刻、完整多渠道数据验收或生产发布。用户最终视觉签收和真实数据场景边界继续保留。
